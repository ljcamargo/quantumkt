class PI: Angle {

    constructor(multiplier: Float?=null, divider: Float?=null) {
        value = Math.PI.toFloat()
        symbol = "PI"
        if (multiplier != null) this.multiplier = multiplier
        if (divider != null) this.dividend = dividend
    }
}